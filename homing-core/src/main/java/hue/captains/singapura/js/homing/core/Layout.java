package hue.captains.singapura.js.homing.core;

/** Layer — Structural primitives — page skeleton (st-root, st-main, st-layout, st-grid, st-list). */
public non-sealed interface Layout extends Layer {}
