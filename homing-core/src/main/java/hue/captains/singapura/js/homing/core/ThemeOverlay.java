package hue.captains.singapura.js.homing.core;

/** Layer — Theme-author final say — textures, body-font overrides, theme-specific structural tweaks. */
public non-sealed interface ThemeOverlay extends Layer {}
